<%= form_for(@frontend) do |f| %>
  <% if @frontend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@frontend.errors.count, "error") %> prohibited this frontend from being saved:</h2>

      <ul>
      <% @frontend.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
